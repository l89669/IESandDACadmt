<Window x:Class="IESandDACadmt.View.WpfLauncher"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IESandDACadmt.View"
        xmlns:locmod="clr-namespace:IESandDACadmt.Model"
        mc:Ignorable="d"
        Title="Database Tool Launcher" Height="350" Width="560">
    <Window.Resources>
        <Style x:Key="myWrap" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <TextBlock Text="{TemplateBinding Content}" FontSize="15" TextWrapping="Wrap" VerticalAlignment="Center" HorizontalAlignment="Center" Background="LightGray"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    <Grid Name="MasterGrid" DataContext="Path=locmod.DbSqlSpController.DbSqlSpControllerData}">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"  Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5,0,5,0">
            <TextBlock Text="Heat Server Type: "/>
            <ListBox Name="ServerType" Width="100"/>
            <Button Name="ChangeType" Content="CHANGE TYPE" Padding="5,0,5,0"/>
        </StackPanel>
        <Grid Name="SqlConnectionTestGrid" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" />
                <RowDefinition Height="1*" />
                <RowDefinition Height="1*" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
        </Grid>
        <Grid Name="LaunchButtonsGrid" Grid.Row="2" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Button Name="LaunchProfiler" Padding="5,0,5,0" Grid.Column="0" Style="{StaticResource myWrap}">LAUNCH PROFILING TOOL</Button>
            <Button Name="LaunchDeletion" Padding="5,0,5,0" Grid.Column="1" Style="{StaticResource myWrap}">LAUNCH DELETION TOOL</Button>
            <Button Name="LaunchHealth" Padding="5,0,5,0" Grid.Column="2" Style="{StaticResource myWrap}">LAUNCH HEALTH TOOL</Button>
        </Grid>
    </Grid>
</Window>
